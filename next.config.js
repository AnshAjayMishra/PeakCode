/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'ik.imagekit.io',
      'tailark.com',
      'html.tailus.io',
    ],
  },
};

module.exports = nextConfig;
